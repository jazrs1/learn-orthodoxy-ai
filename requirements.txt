fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-dotenv>=1.0,<2.0
openai>=1.75,<2.0
chromadb==0.6.3  # pinned: local and Railway must write the same on-disk format (INGEST_PLAN.md 9.3)
posthog>=2.4.0,<6.0.0
requests>=2.32,<3.0
beautifulsoup4>=4.12,<5.0
pypdf==5.9.0  # pinned: v1-legacy byte-exactness and the Arabic text depend on its output (ING-002)
# Ingestion only (python -m ingestion); never imported by api.py. PyMuPDF is AGPL-3.0 (ING-001).
pymupdf==1.28.2
tiktoken>=0.8,<1.0
