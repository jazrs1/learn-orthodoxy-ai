# Learn Orthodoxy Frontend

The Next.js frontend is intended for Vercel deployment and expects:

- `POSTGRES_URL` for durable chat storage
- `ORTHODOX_API_URL` for the server-side chat proxy
- `NEXT_PUBLIC_API_URL` for browser-side saint search requests

See the repository root `README.md` for full local development and deployment instructions.
